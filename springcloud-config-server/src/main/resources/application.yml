server: 
  port: 3344 
  
spring:
  application:
    name:  springcloud-config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:loxn/springcloud-config-server.git #GitHub上面的git仓库名字
 

# 启动这个工程，访问 localhost:3344/application-dev.yml
